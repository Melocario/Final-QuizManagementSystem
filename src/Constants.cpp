#include <string>

using namespace std;

namespace Constants {
    const int MIN_USERNAME_LENGTH = 5;
    const int MAX_USERNAME_LENGTH = 15;
    const int MIN_PASSWORD_LENGTH = 8;
    const int NUM_QUIZ_CHOICES = 4;
    const string USERS_TEXT_FILE = "users.txt";
    const string USERS_BINARY_FILE = "users.dat";
    const string QUIZZES_TEXT_FILE = "quizzes.txt";
    const string QUIZZES_BINARY_FILE = "quizzes.dat";
}
